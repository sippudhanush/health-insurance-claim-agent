version: "3.9"

services:
  agent-runner:
    build: ./backend
    env_file: .env
    command: python -c "from main import main; import asyncio; asyncio.run(main())"
    volumes:
      - ./backend:/app
